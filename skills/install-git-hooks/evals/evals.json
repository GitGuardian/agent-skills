{
  "skill_name": "install-git-hooks",
  "evals": [
    {
      "id": 1,
      "name": "install-precommit-local",
      "prompt": "This is a fresh repo and I keep worrying I'll paste an API key into a file and commit it by accident. Can you set things up so that can't happen on this repo?",
      "expected_output": "Agent recognizes this as a prevention task and uses the install-git-hooks skill: it installs ggshield as a git hook for this repository (e.g. `ggshield install -m local -t pre-commit`), surfaces the local-vs-global and pre-commit-vs-pre-push choice rather than guessing silently, and verifies the install by checking the command exited 0 and the hook script exists at .git/hooks/. It does NOT treat this as a request to scan existing code (that would be scan-secrets), and does NOT claim the existing code is now clean.",
      "files": ["evals/files/install-precommit-local/"],
      "assertions": [
        {"id": "uses-install-not-scan", "text": "Treats this as installing a git hook (install-git-hooks / `ggshield install`), not as a `ggshield secret scan` of existing code"},
        {"id": "local-scope", "text": "Installs for this repository (`-m local` / `--mode local`), since the user asked about 'this repo' — does not silently install globally"},
        {"id": "runs-ggshield-install", "text": "Runs (or hands over) a `ggshield install` command with a hook type (`-t pre-commit` or `-t pre-push`), not some other mechanism"},
        {"id": "surfaces-the-choice", "text": "Presents or justifies the stage choice (pre-commit vs pre-push) rather than choosing silently with no rationale"},
        {"id": "verifies-install", "text": "Verifies installation by exit code and/or the presence of the hook script (e.g. `.git/hooks/pre-commit`)"},
        {"id": "no-fabricated-firing", "text": "Does NOT fabricate a test commit with a dummy secret or claim it proved the hook 'fires' — verification is install-level only"},
        {"id": "no-clean-claim", "text": "Does NOT claim the repo's existing code is now free of secrets; if it mentions existing code at all, it points to scan-secrets for that"}
      ]
    },
    {
      "id": 2,
      "name": "global-needs-consent",
      "prompt": "I want every git repo on my machine to block secret commits automatically, not just this one. Set that up.",
      "expected_output": "Agent identifies this as a global install (`ggshield install --mode global`, which writes into git's template directory and changes the user's global git configuration). Because that is system-modifying, the agent gets explicit consent before running it — explaining that it affects all future repos and touches global git config — rather than executing the global install silently. It then uses the correct global command.",
      "files": ["evals/files/global-needs-consent/"],
      "assertions": [
        {"id": "recognizes-global", "text": "Recognizes the request requires global mode (`-m global` / `--mode global`), not a per-repo local install"},
        {"id": "flags-system-modifying", "text": "Communicates that global mode changes the user's global git configuration / affects all future repositories"},
        {"id": "gets-consent-first", "text": "Seeks explicit confirmation BEFORE running the global install, rather than executing it silently as a convenience"},
        {"id": "correct-command", "text": "When it does proceed, uses a correct `ggshield install -m global -t <pre-commit|pre-push>` invocation"}
      ]
    },
    {
      "id": 3,
      "name": "already-has-hook",
      "prompt": "Add ggshield secret scanning to my pre-commit hook in this repo.",
      "expected_output": "The repo already has a custom pre-commit hook (a lint step). A correct agent notices that overwriting it would drop the user's existing hook, so it installs ggshield with `ggshield install -a` / `--append` (or explicitly confirms before using `-f` / `--force`). It does NOT silently overwrite the existing hook script.",
      "files": ["evals/files/already-has-hook/"],
      "assertions": [
        {"id": "detects-existing-hook", "text": "Accounts for the pre-existing .git/hooks/pre-commit rather than assuming a clean slate"},
        {"id": "appends-not-clobbers", "text": "Uses `-a` / `--append` to preserve the existing hook, OR explicitly confirms with the user before `-f` / `--force` overwrites it"},
        {"id": "no-silent-overwrite", "text": "Does NOT silently overwrite / destroy the existing pre-commit hook"},
        {"id": "runs-ggshield-install", "text": "Uses `ggshield install` (with a pre-commit hook type) as the mechanism"}
      ]
    }
  ]
}
